import { addToStorage } from './helperFunction/helperIndex.js';
const addButton = document.getElementById('add');
addButton === null || addButton === void 0 ? void 0 : addButton.addEventListener('click', addToStorage);
